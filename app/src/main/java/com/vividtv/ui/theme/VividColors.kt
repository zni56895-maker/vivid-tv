package com.vividtv.ui.theme

import androidx.compose.runtime.Composable
import androidx.compose.ui.graphics.Color

// ── 全暗色调色板 ──
object VividColors {
    // Background
    val BackgroundDarkest = Color(0xFF0D0D0D)
    val BackgroundDark = Color(0xFF1A1A1A)
    val BackgroundMedium = Color(0xFF2A2A2A)
    val BackgroundCard = Color(0xFF1E1E1E)
    val BackgroundCardFocused = Color(0xFF2E2E2E)

    // Accent
    val AccentRed = Color(0xFFE50914)
    val AccentBlue = Color(0xFF1A9FFF)
    val AccentBlueDark = Color(0xFF0D6EFD)
    val AccentGreen = Color(0xFF4CAF50)

    // Text
    val TextPrimary = Color(0xFFE0E0E0)
    val TextSecondary = Color(0xFF9E9E9E)
    val TextDisabled = Color(0xFF616161)

    // Focus / Glow
    val FocusBorder = Color(0xFF1A9FFF)
    val FocusGlow = Color(0x331A9FFF)

    // Divider
    val Divider = Color(0xFF333333)
    val DividerAccent = Color(0xFFE50914)

    // Scrim
    val scrim = Color(0x99000000)
    val Transparent = Color(0x00000000)

    // Status
    val Success = Color(0xFF4CAF50)
    val Warning = Color(0xFFFF9800)
    val Error = Color(0xFFF44336)
}
